{% assign index_length = include.index | append: '' | size %}{% assign title_length = include.title | replace: '&amp;', '&' | size %}{% assign info_length = include.info | replace: '&amp;', '&' | size %}{% assign max_index_length = include.max_index | append: '' | size %}{% assign index_padding = max_index_length | minus: index_length %}{% assign padding = 75 | minus: title_length | minus: max_index_length | minus: info_length %}{% for num in (1..index_padding) %}0{% endfor %}{{ include.index }} - {{ include.title | replace: '&amp;', '&' }}{% for num in (1..padding) %}{{ ' ' }}{% endfor %}({{ include.info | replace: '&amp;', '&' }})
